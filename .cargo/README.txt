# Deze map wordt automatisch gevuld door setup-libopus-windows.ps1
# (op Windows) of blijft leeg op Linux (waar pkg-config het werk doet).
#
# Niet handmatig committen aan git als config.toml hier verschijnt — die
# bevat absolute paden die machine-specifiek zijn.
